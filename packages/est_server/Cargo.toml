[package]
name = "est_server"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.3"
est_core = { version = "*", path = "../est_core" }
serde = { version = "1.0.219", features = ["derive"] }
tokio = { version = "1.44.2", features = ["macros", "rt-multi-thread"] }
toml = "0.8.20"
